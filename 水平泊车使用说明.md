# 水平泊车（侧方位停车）仿真系统使用说明

## 项目概述

本项目实现了基于ROS的水平泊车（侧方位停车）仿真系统，使用真实OSM地图数据，从lane119出发到lane121开始泊车，根据停车位边界（way 10295和10298）计算揉库轨迹。

## 系统特点

### 1. **真实地图数据**
- 使用`parking_map.osm`地图文件
- 起始车道: **lane119** (relation 119)
- 泊车计算点: **lane121** (relation 121)
- 停车位边界:
  - **way 10295**: 前边界线（节点10293→10294）
  - **way 10298**: 后边界线（节点10296→10297）

### 2. **基于真实驾驶逻辑的轨迹规划**
实现了4阶段真实侧方位停车算法（"让车尾先到位，车头再跟进"）：
- **阶段1**: 对齐停车位 - 车身与路沿平行，横向保持0.5m
- **阶段2**: 45°倒车 - 方向盘右打死，倒车至车身45°
- **阶段3**: 反向45°倒车 - 方向盘左打死，车身回到与lane121平行
- **阶段4**: 沿中心线直线倒车至中心，完成最终定位

### 3. **车辆参数配置**
- 轴距: 1.8m
- 车长: 2.4m
- 车宽: 1.4m
- 最大转向角: 0.6 rad (约34度)

### 4. **停车位约束计算**
- 自动计算停车位中心位置
- 自动计算停车位朝向和尺寸
- 基于车辆尺寸验证停车位是否足够

## 文件结构

```
park_ws/
├── src/parking_demo/
│   ├── launch/
│   │   ├── parking.launch                      # 原垂直泊车launch
│   │   └── horizontal_parking.launch           # 水平泊车launch（新建）
│   ├── maps/
│   │   ├── parking_map.osm                     # 地图文件
│   │   ├── map_config.yaml                     # 地图配置
│   │   └── map_projector_info.yaml
│   ├── src/
│   │   ├── parking_system_node_refactored.cpp  # 原垂直泊车节点
│   │   └── horizontal_parking_node.cpp         # 水平泊车节点（新建）
│   ├── include/parking_demo/                   # 共享头文件
│   └── CMakeLists.txt                          # 已更新
└── 水平泊车使用说明.md                          # 本文件
```

## 编译方法

```bash
cd /home/goldminer/park_ws
catkin_make
source devel/setup.bash
```

## 运行仿真

### 方法1：直接启动节点

```bash
# 启动ROS核心
roscore &

# 启动水平泊车节点
roslaunch parking_demo horizontal_parking.launch
```

### 方法2：使用RViz可视化

```bash
# 终端1：启动ROS核心
roscore

# 终端2：启动水平泊车节点
roslaunch parking_demo horizontal_parking.launch

# 终端3：启动RViz
rviz -d src/parking_demo/parking_demo.rviz
```

## 可视化主题

运行后，系统会发布以下ROS话题：

| 话题名称 | 消息类型 | 说明 |
|---------|---------|------|
| `/osm_ways` | `visualization_msgs/MarkerArray` | 道路边界线 |
| `/osm_relations` | `visualization_msgs/MarkerArray` | 车道区域 |
| `/planned_path` | `nav_msgs/Path` | 从lane119到lane121的规划路径 |
| `/parking_trajectory` | `nav_msgs/Path` | 侧方位停车轨迹 |
| `/parking_slot_marker` | `visualization_msgs/MarkerArray` | 停车位边界和目标位置 |
| `/vehicle_pose` | `geometry_msgs/Pose2D` | 车辆位姿 |
| `/cmd_vel` | `geometry_msgs/Twist` | 控制指令 |
| **TF**: `map → base_link` | - | 车辆坐标变换 |

## 参数配置

可以在`launch/horizontal_parking.launch`中调整以下参数：

### 车辆参数
```xml
<param name="vehicle_wheelbase" value="1.8" />    <!-- 轴距 -->
<param name="vehicle_length" value="2.4" />       <!-- 车长 -->
<param name="vehicle_width" value="1.4" />        <!-- 车宽 -->
<param name="vehicle_max_steering" value="0.6" /> <!-- 最大转向角 -->
```

### 控制参数
```xml
<param name="lookahead_distance" value="2.0" />   <!-- 前瞻距离 -->
<param name="max_speed" value="1.5" />            <!-- 最大速度 -->
<param name="min_speed" value="0.3" />            <!-- 最小速度 -->
<param name="goal_tolerance" value="0.15" />      <!-- 目标容差 -->
```

### 泊车参数
```xml
<param name="parallel_parking_angle_1" value="0.52" />  <!-- 阶段1转向角（约30度） -->
<param name="parallel_parking_angle_2" value="-0.52" /> <!-- 阶段2转向角（约-30度） -->
<param name="parking_speed" value="0.4" />              <!-- 泊车速度 -->
```

### 地图ID参数（硬编码在源文件中）
```cpp
static constexpr long long START_LANELET_ID = 119;     // 起始车道
static constexpr long long PARKING_LANELET_ID = 121;   // 泊车计算起点
static constexpr long long PARKING_BOUNDARY_WAY_1 = 10295;  // 前边界
static constexpr long long PARKING_BOUNDARY_WAY_2 = 10298;  // 后边界
```

## 系统状态机

```
APPROACHING（接近阶段）
    ↓ 到达lane121
PARKING（泊车阶段）
    ↓ 完成揉库
COMPLETED（完成）
```

### 1. APPROACHING阶段
- 使用Pure Pursuit控制器跟随全局路径
- 从lane119行驶到lane121
- 到达后自动切换到PARKING阶段

### 2. PARKING阶段
- 生成4阶段真实侧方位停车轨迹
- 执行揉库操作：
  - 阶段1: 对齐确认（1步）
  - 阶段2: 45°倒车右转（35步）
  - 阶段3: 反向45°倒车左转（40步）
  - 阶段4: 沿中心线直线倒车完成最终定位
- 总计约101个轨迹点

### 3. COMPLETED阶段
- 车辆停止
- 持续发布最终位姿

## 调试方法

### 1. 查看日志输出

```bash
# 查看节点日志
roslaunch parking_demo horizontal_parking.launch

# 关键日志信息：
# - "Parking slot computed": 停车位尺寸和位置
# - "Path planned successfully": 路径规划成功
# - "Phase 1/2/3": 各阶段轨迹生成
# - "Reached parking start position": 到达泊车起点
# - "Parking Completed": 泊车完成
```

### 2. 检查话题数据

```bash
# 查看所有话题
rostopic list

# 查看停车位标记
rostopic echo /parking_slot_marker

# 查看车辆位姿
rostopic echo /vehicle_pose

# 查看泊车轨迹
rostopic echo /parking_trajectory
```

### 3. 可视化检查

在RViz中添加以下显示：
- **MarkerArray**: `/osm_ways`, `/osm_relations`, `/parking_slot_marker`
- **Path**: `/planned_path`, `/parking_trajectory`
- **Pose**: 添加`/vehicle_pose`（需要转换为PoseStamped）
- **TF**: 显示`map`和`base_link`坐标系

## 常见问题

### 1. 编译错误
```bash
# 清理并重新编译
cd /home/goldminer/park_ws
catkin_make clean
catkin_make
source devel/setup.bash
```

### 2. 找不到地图文件
确认文件存在：
```bash
ls -lh src/parking_demo/maps/parking_map.osm
```

### 3. 路径规划失败
检查lanelet ID是否存在：
```bash
grep 'relation id="119"' src/parking_demo/maps/parking_map.osm
grep 'relation id="121"' src/parking_demo/maps/parking_map.osm
```

### 4. 停车位边界未找到
检查way ID是否存在：
```bash
grep 'way id="10295"' src/parking_demo/maps/parking_map.osm
grep 'way id="10298"' src/parking_demo/maps/parking_map.osm
```

### 5. 车辆不动
- 检查控制器是否正确初始化
- 查看日志中的"Vehicle initialized at path start"信息
- 确认全局路径非空

## 技术实现细节

### 1. 停车位几何计算
```cpp
// 从way ID获取边界节点坐标
boundary_1: (node 10293) → (node 10294)
boundary_2: (node 10296) → (node 10297)

// 计算停车位中心
center_x = (b1_start_x + b1_end_x + b2_start_x + b2_end_x) / 4
center_y = (b1_start_y + b1_end_y + b2_start_y + b2_end_y) / 4

// 计算停车位朝向（平行于边界线）
theta = atan2(b1_end_y - b1_start_y, b1_end_x - b1_start_x)

// 计算停车位尺寸
length = hypot(b1_end - b1_start)
width = distance(boundary_1_center, boundary_2_center)
```

### 2. 真实侧方位停车轨迹生成（4阶段法）

#### 阶段1: 对齐确认
```cpp
// 车辆已通过全局路径到达停车位旁边
// 车身与停车位平行，横向距离约0.5m
trajectory.push_back(start);  // 初始位置
```

#### 阶段2: 45°倒车（方向盘右打死）
```cpp
// 目标：车身与路沿形成45°夹角，车尾进入车位2/3
double R = min_turn_radius * 1.1;  // 转弯半径
double max_steering = atan(wheelbase / R);  // 最大转向角

for (int i = 1; i <= 35; ++i) {
    double angle_change = (i / 35.0) * 0.785;  // 45° = π/4
    theta = start_theta + angle_change;  // 车头向右转
    // 后轴中心沿圆弧倒车运动
    x = start_x - back_distance * cos(start_theta) + R * (sin(theta) - sin(start_theta));
    y = start_y - back_distance * sin(start_theta) - R * (cos(theta) - cos(start_theta));
    v = -0.4;  // 倒车速度
    phi = max_steering;  // 右打死
}
```

#### 阶段3: 反向45°倒车（方向盘左打死）
```cpp
// 目标：车尾贴近路沿，车身回到平行状态
for (int i = 1; i <= 40; ++i) {
    double angle_change = (i / 40.0) * 0.785;  // 再转45°
    theta = phase2_end_theta - angle_change;  // 车头向左转
    // 继续圆弧倒车，但转向相反
    x = phase2_end_x - back_distance * cos(phase2_end_theta) - R * (sin(theta) - sin(phase2_end_theta));
    y = phase2_end_y - back_distance * sin(phase2_end_theta) + R * (cos(theta) - cos(phase2_end_theta));
    v = -0.35;
    phi = -max_steering;  // 左打死
}
```

#### 阶段4: 沿中心线直线倒车到中心
```cpp
// 直线移动到停车位中心，保持平行
double dist_to_center = hypot(target_x - phase3_end_x, target_y - phase3_end_y);
int steps = max(20, static_cast<int>(dist_to_center / 0.08));
for (int i = 1; i <= steps; ++i) {
    double alpha = i / static_cast<double>(steps);
    x = phase3_end_x + (target_x - phase3_end_x) * alpha;
    y = phase3_end_y + (target_y - phase3_end_y) * alpha;
    theta = target_theta;  // 保持目标姿态
    v = -0.3;  // 慢速倒车
    phi = 0.0;  // 直线
}
```

### 3. 关键几何原理

**为什么是45°？**
- 45°是侧方位停车的最优角度，既能让车尾充分进入车位，又为车头摆正留足空间
- 角度太小（<30°）：车尾进入不够深，后续需要多次调整
- 角度太大（>60°）：车头摆动空间不足，容易碰到前车

**转弯半径选择：**
```cpp
R = min_turn_radius * 1.1 = (wheelbase / tan(max_steering)) * 1.1
```
- 基准值：根据车辆动力学计算的最小转弯半径
- 安全系数1.1：避免轮胎或轮毂刮蹭路沿石

**"让车尾先到位，车头再跟进"的实现：**
1. 阶段2: 车尾沿大圆弧后退，进入停车位（尾部先到位）
2. 阶段3: 反向转向，车头随着车尾的圆弧运动自然甩入（头部跟进）
3. 阶段4: 直接沿中心线直线倒车到位

### 3. Pure Pursuit控制器
- 前瞻距离: 2.0m（针对侧方位停车优化）
- 速度控制: 0.3-1.5 m/s
- 目标容差: 0.15m（高精度）
- 控制频率: 20Hz

## 性能指标

- **路径规划时间**: < 100ms
- **轨迹生成时间**: < 10ms
- **控制循环频率**: 20Hz
- **可视化刷新频率**: 2Hz
- **轨迹点数**: 约75个点
- **泊车时间**: 约3-4秒（仿真时间）

## 下一步改进建议

1. **动态障碍物检测**: 集成激光雷达/相机数据
2. **自适应揉库**: 根据停车位实际尺寸动态调整阶段参数
3. **多次揉库支持**: 如果一次未成功，支持多次调整
4. **路径优化**: 使用Hybrid A*或RRT*生成更优轨迹
5. **MPC控制器**: 替换Pure Pursuit，提供更好的跟踪性能
6. **实车验证**: 在真实车辆上测试和调优

## 贡献者

- 基于现有parking_demo项目开发
- 新增水平泊车功能：horizontal_parking_node.cpp
- 新增launch文件：horizontal_parking.launch

## 许可证

遵循原项目许可证

---

**最后更新**: 2025-12-28
**版本**: 1.0.0
